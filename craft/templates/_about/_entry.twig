{% extends "_layout" %}
{% import '_macro/_assets' as assets %}

{% set breadcrumbs = [
    craft.entries.section('about').first(),
    entry,
] %}

{% block content %}
    <div class="container about">
        <div class="grid">
            <div class="grid__12 grid--bp-med__3">
                {% include '_asideNav' with {'about': true} %}
            </div>
            <div class="grid__12 grid--bp-med__9">
                <div class="section section--default">
                    <div class="container">
                        {% include '_snippet/_content/_pageTitle' %}
                        {% include '_snippet/_content/_intro' %}
                    </div>
                </div>

                <div class="section">
                    <div class="container ">
                        <div>
                            {% include '_snippet/_content/_contentBuilder' %}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}